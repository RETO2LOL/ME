{
  "name": "contact-form-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker verifying Google reCAPTCHA v3 and forwarding contact form emails.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
